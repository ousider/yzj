package pigfarm.portal;


import org.junit.Test;

import pigfarm.BasicTest;

//import xn.pigfarm.service.portal.ICdRoleService;

/**
 * Created Eclipse Java EE. User:li.zhou Date:2016-4-9 Time:14:03:08
 */
public class CdOroleTest extends BasicTest {
    //
    // @Autowired
    // private ICdRoleService cdRoleService;


    // @Autowired
    // private ICdRoleService cdRoleService;
    //
    // @Test
    // public void test() {
    // // 删除
    // // assertTrue(cdOroleService.deleteByPrimaryKey(1) > 0);
    //
    // // 增加
    // // CdRoleModel entity=new CdRoleModel();
    // // assertTrue(cdOroleService.insert(entity) > 0);
    // // assertTrue(cdOroleService.insertSelective(entity) > 0);
    //
    // // 修改
    // // CdRoleModel updateentity=new CdRoleModel();
    // // assertTrue(cdOroleService.updateByPrimaryKeySelective(updateentity) >
    // // 0);
    // // assertTrue(cdOroleService.updateByPrimaryKey(updateentity) > 0);
    //
    // // 查询
    // // assertNotNull(cdOroleService.selectByPrimaryKey(1));
    //
    // List<CdRoleModel> rolelist = cdRoleService.searchUserByResources();
    //
    // for (CdRoleModel CdRoleModel : rolelist) {
    // System.out.println(CdRoleModel.getRoleName() + " " + CdRoleModel.getRowId());
    // Iterator<CdModuleModel> iterator = CdRoleModel.getCdModule().iterator();
    // while (iterator.hasNext()) {
    // CdModuleModel CdModuleModel = (CdModuleModel) iterator.next();
    // System.out.println(CdModuleModel.getRowId());
    // }
    // }
    //
    // List<CdRoleModel> rolelist1 = cdRoleService.searchUserByRole("sysadmin");
    // for (Iterator<CdRoleModel> iterator = rolelist1.iterator(); iterator.hasNext();) {
    // CdRoleModel CdRoleModel = iterator.next();
    // System.out.println(CdRoleModel.getRoleName());
    // System.out.println(CdRoleModel.getItercept_url());
    // }
    //
    // }


	@Test
	public void test() {
		// 删除
		// assertTrue(cdOroleService.deleteByPrimaryKey(1) > 0);

		// 增加
		// CdRoleModel entity=new CdRoleModel();
		// assertTrue(cdOroleService.insert(entity) > 0);
		// assertTrue(cdOroleService.insertSelective(entity) > 0);

		// 修改
		// CdRoleModel updateentity=new CdRoleModel();
		// assertTrue(cdOroleService.updateByPrimaryKeySelective(updateentity) >
		// 0);
		// assertTrue(cdOroleService.updateByPrimaryKey(updateentity) > 0);

		// 查询
		// assertNotNull(cdOroleService.selectByPrimaryKey(1));

        // List<CdRoleModel> rolelist = cdRoleService.searchUserByResources();

        // for (CdRoleModel CdRoleModel : rolelist) {
        // System.out.println(CdRoleModel.getRoleName() + " " + CdRoleModel.getRowId());
        // Iterator<CdModuleModel> iterator = CdRoleModel.getCdModule().iterator();
        // while (iterator.hasNext()) {
        // CdModuleModel CdModuleModel = (CdModuleModel) iterator.next();
        // System.out.println(CdModuleModel.getRowId());
        // }
        // }
        //
        // List<CdRoleModel> rolelist1 = cdRoleService.searchUserByRole("sysadmin");
        // for (Iterator<CdRoleModel> iterator = rolelist1.iterator(); iterator.hasNext();) {
        // CdRoleModel CdRoleModel = iterator.next();
        // System.out.println(CdRoleModel.getRoleName());
        // System.out.println(CdRoleModel.getItercept_url());
        // }
        //
	}


}
